# --- Core ---
numpy>=2.4.2       # Generación de datos estadísticos
pydantic>=2.12.5     # Validación de Schemas (Schema-on-Write)

# --- API & Web ---
flask>=3.1.2        # Receptor HTTP
python-dotenv>=1.2.1 # Manejo de variables de entorno

# --- Storage ---
boto3>=1.40.70       # Cliente para MinIO/S3

# --- Testing & Scripts ---
requests>=2.32.5    # Para scripts de prueba (test_ingestion_manual.py)
pytest>=9.0.2       # Para tests unitarios automatizados
tqdm>=4.67.1        # Para visualización de barras de progreso en los tests

# --- ETL & Silver Layer ---
pandas>=2.3.3       # Procesamiento de DataFrames
pyarrow>=22.0.0
fsspec>=2026.1.0   # Gestión de sistemas de archivos
s3fs>=2026.1.0     # Soporte S3 para Pandas/Fsspec

# --- Agregaciones y Métricas de Negocios: Silver layer to Gold layer ---
python-duckdb>=1.4.3 

# --- Conexión y procesamiento distribuido con PySpark en comunicación con S3 (MinIO) ---
pyspark=3.5.8 # Motor distribuido local (JVM via OpenJDK 17)
jupyter
openjdk=17